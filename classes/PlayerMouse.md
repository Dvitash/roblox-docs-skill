# PlayerMouse

**Superclass:** [Mouse](Mouse.md)

The `PlayerMouse` is a legacy API that behaves identically to the `Class.Mouse` object obtained via `Class.Tool.Equipped`, and developers are advised to use the new `Class.UserInputService`.

## Tags
- NotCreatable
