# Rotate

**Superclass:** [JointInstance](JointInstance.md)

The `Rotate` object is a class used to allow rotation between two parts, commonly achieved via `Enum.SurfaceType.Hinge` or by defining an arbitrary axis and point of rotation.

## Tags
- Deprecated
