# Backpack

**Superclass:** [Instance](Instance.md)

The `Backpack` class is a container for a player's inventory, where `Class.Tool` objects display as icons when a player's `Class.StarterPack` or `Class.StarterGear` are added.
