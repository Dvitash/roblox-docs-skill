# CylinderMesh

**Superclass:** [BevelMesh](BevelMesh.md)

The CylinderMesh object applies a 'cylinder' mesh to a `Class.BasePart` by aligning the height with the part's Y axis, and it provides exposure to `Class.DataModelMesh` properties for scaling and offsetting.

## Tags
- Deprecated
