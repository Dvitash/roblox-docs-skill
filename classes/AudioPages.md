# AudioPages

**Superclass:** [Pages](Pages.md)

A special version of `Class.Pages` was returned by `Class.AssetService:SearchAudioAsync()`. This class inherits from `Pages` and has specific properties, methods, and events.

## Tags
- NotCreatable
- NotReplicated
