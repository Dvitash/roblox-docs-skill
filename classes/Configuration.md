# Configuration

**Superclass:** [Instance](Instance.md)

Configuration objects are containers used to hold value objects, making values accessible to `Class.Tool|Tools` and models using `Class.Script|Scripts`.
