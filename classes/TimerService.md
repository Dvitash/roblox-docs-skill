# TimerService

**Superclass:** [Instance](Instance.md)

This file describes the `TimerService` class, which is an internal service responsible for scheduling timed events. It is used by `Class.Debris` and is not accessible to developers.

## Tags
- NotCreatable
- Service
