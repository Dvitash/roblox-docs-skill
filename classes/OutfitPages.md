# OutfitPages

**Superclass:** [Pages](Pages.md)

The `OutfitPages` class is a special version of the `Class.Pages` class used to iterate over a user's outfits, which is included in the `Class.AvatarEditorService:GetOutfitsAsync()`.

## Tags
- NotCreatable
- NotReplicated
