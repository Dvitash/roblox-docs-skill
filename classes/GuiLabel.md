# GuiLabel

**Superclass:** [GuiObject](GuiObject.md)

GuiLabel is an abstract class for non-interactive 2D user interface elements that do not register click events. It inherits from `GuiObject` and serves as the base class for `Class.ImageLabel` and `Class.TextLabel`.

## Tags
- NotCreatable
