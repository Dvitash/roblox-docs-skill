# TextChatConfigurations

**Superclass:** [Instance](Instance.md)

The `TextChatConfigurations` class defines the parental class for all text chat configuration settings, including `Class.BubbleChatConfiguration` and `Class.ChatWindowConfiguration`.

## Tags
- NotCreatable
- NotReplicated
