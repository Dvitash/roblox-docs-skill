# CacheableContentProvider

**Superclass:** [Instance](Instance.md)

CacheableContentProvider is a variant of `Class.ContentProvider` that caches assets received by an instance, which is used by other classes to manage content.

## Tags
- NotCreatable
- Service
- NotReplicated
