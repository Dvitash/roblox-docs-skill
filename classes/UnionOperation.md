# UnionOperation

**Superclass:** [PartOperation](PartOperation.md)

UnionOperation is the result of parts being joined together into a single solid model using Studio's tools or `Class.BasePart:UnionAsync()`/`Class.BasePart:SubtractAsync()`.
