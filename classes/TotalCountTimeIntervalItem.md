# TotalCountTimeIntervalItem

**Superclass:** [StatsItem](StatsItem.md)

The `TotalCountTimeIntervalItem` is a special type of `Class.StatsItem` that measures the total count over time, which is currently unused.

## Tags
- NotCreatable
