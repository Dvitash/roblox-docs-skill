# WorldModel

**Superclass:** [WorldRoot](WorldRoot.md)

WorldModel is a class that extends limited physics for its children when parented to a `Class.ViewportFrame`. It allows for spatial animation and spatial querying, while avoiding unnecessary simulation.
